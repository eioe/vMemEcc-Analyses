dev_hor = rollmean(na.spline(dev_hor), 5,
na.pad = TRUE))
tmp_plot <- cdat %>%
#  filter(block_num < 12) %>%
mutate(gaze_dev_hor = (gaze_dev_hor - bl_val)) %>%
group_by(c_CueDir, time_resampled, eye) %>%
summarise(dev_hor = mean(gaze_dev_hor, na.rm = FALSE),
sd = sd(gaze_dev_hor, na.rm = FALSE),
min = dev_hor - sd,
max = dev_hor + sd,
.groups = "keep") %>%
mutate(c_CueDir = as_factor(c_CueDir)) # ,
tmp_plot %>%  ggplot(aes(x = time_resampled, y = dev_hor, col = c_CueDir)) +  geom_point() +
ylim(c(-1,1)) +
xlim(c(-1.1, 2.2))
View(tmp_plot)
library(tidyverse)
source("EyeTracking/resample_gaze_data.R")
data_from_hdd <- TRUE
if (data_from_hdd) {
path_data <- file.path('E:', 'vMemEcc', 'SubjectData_extern', 'SubjectData')
} else {
path_data <- here('..', '..', 'Data', 'SubjectData')
}
# load subject list and exclude bad subjects:
sub_ids <- str_c('VME_S', str_pad(setdiff(1:27, c(11,14,19)),
2, 'left', 0))
sub_id <- sub_ids[1]
path_data_sub <- file.path(path_data, sub_id, 'EyeTracking', 'R_data')
fname <- file.path(path_data_sub, glue("dataresampled-{sub_id}.rds"))
library(glue)
fname <- file.path(path_data_sub, glue("dataresampled-{sub_id}.rds"))
data <- readRDS(fname)
saccdat <- readRDS(file.path(path_data_sub, glue('saccdata_{sub_id}.rds')))
unique(data$sub_id)
min(data$time_resampled)
max(data$time_resampled)
saccdat <- saccdat %>%
# ungroup() %>%
# mutate(block = as.character(block)) %>%
select(sub_id, block, trial, reject_eeg) %>%
mutate(block_nr = block) %>% # , # as.character(block),
#trial = as.character(trial)) %>%
group_by(sub_id, block_nr, trial) %>%
summarise(reject_eeg = any(reject_eeg)) # %>%
# mutate(rej_eeg = as.character(reject_eeg)) %>%
# ungroup() %>%
# distinct(sub_id, block_nr, trial, .keep_all = TRUE)
data_joined <- data %>%
#mutate(trial = as.character(trial)) %>%
#ungroup() %>%
left_join(saccdat) %>%
mutate(reject_eeg = if_else(is.na(reject_eeg), FALSE, reject_eeg))
View(data_joined)
View(saccdat)
ha <- distinct(data_joined, block_nr)
View(ha)
? distinct
ha <- distinct(data_joined, block_nr, trial)
ha <- distinct(data_joined, block_nr, trial, eye)
ha <- distinct(data_joined, block_nr, trial, eye, sub_id)
ha <- data_joined %>%  ungroup() %>%  distinct(block_nr)
ha <- data_joined %>%  ungroup() %>%  distinct(block_nr, trial) %>%  filter(reject_eeg)
ha <- data_joined %>%  ungroup() %>%  distinct(block_nr, trial, .keep_all = T) %>%  filter(reject_eeg)
data <- readRDS(fname)
saccdat <- readRDS(file.path(path_data_sub, glue('saccdata_{sub_id}.rds')))
saccdat <- saccdat %>%
# ungroup() %>%
# mutate(block = as.character(block)) %>%
select(sub_id, block, trial, reject_eeg) %>%
mutate(block_nr = block) %>% # , # as.character(block),
#trial = as.character(trial)) %>%
group_by(sub_id, block_nr, trial) %>%
summarise(reject_eeg = any(reject_eeg)) # %>%
saccdat <- readRDS(file.path(path_data_sub, glue('saccdata_{sub_id}.rds')))
saccdat <- saccdat %>%
# ungroup() %>%
# mutate(block = as.character(block)) %>%
select(sub_id, block, trial, reject_eeg) %>%
mutate(block_nr = block) %>% # , # as.character(block),
#trial = as.character(trial)) %>%
group_by(sub_id, block_nr, trial) %>%
summarise(reject_eeg = any(reject_eeg)) %>%
# mutate(rej_eeg = as.character(reject_eeg)) %>%
ungroup()
# distinct(sub_id, block_nr, trial, .keep_all = TRUE)
data_joined <- data %>%
#mutate(trial = as.character(trial)) %>%
ungroup() %>%
left_join(saccdat) %>%
mutate(reject_eeg = if_else(is.na(reject_eeg), FALSE, reject_eeg))
ha <- data_joined %>%  ungroup() %>%  distinct(block_nr, trial, .keep_all = T) %>%  filter(reject_eeg)
holder <- list()
for (sub_id in sub_ids) {
path_data_sub <- file.path(path_data, sub_id, 'EyeTracking', 'R_data')
fname <- file.path(path_data_sub, glue("dataresampled-{sub_id}.rds"))
data <- readRDS(fname)
saccdat <- readRDS(file.path(path_data_sub, glue('saccdata_{sub_id}.rds')))
saccdat <- saccdat %>%
select(sub_id, block, trial, reject_eeg) %>%
mutate(block_nr = block) %>%
group_by(sub_id, block_nr, trial) %>%
summarise(reject_eeg = any(reject_eeg)) %>%
ungroup()
data_joined <- data %>%
ungroup() %>%
left_join(saccdat) %>%
mutate(reject_eeg = if_else(is.na(reject_eeg), FALSE, reject_eeg))
holder[[sub_id]] <- data_joined
}
data_joined
monster <- bind_rows(holder, .id = 'sub_id')
alldf_0 <- monster %>%
#left_join(shw) %>%
#choose eye & exp blocks
filter(block_nr != "Block2",
!reject_eeg) %>%
# downsample
# slice(which(row_number() %% 2 == 1)) %>%
# choose cols
select(time_resampled,
block_nr,
trial,
sub_id,
gaze_dev_hor,
confidence,
eye) %>%
# adapt vars
mutate(block_num = parse_number(block_nr),
trial_num_in_block = as.integer(trial),
ppid = sub_id,
.keep = "unused") #%>%
bl <- alldf_0 %>%
dplyr::filter(((time_resampled > -1.1) & (time_resampled < -0.8))) %>%
group_by(ppid, block_num, trial_num_in_block, eye) %>%
summarise(bl_val = mean(gaze_dev_hor, na.rm = FALSE)) %>%
ungroup()
# Combine with behavioral & experimental data
data_behav <- readRDS(file.path(here(), '..', '..', 'Data', 'DataR', "fulldat_behav.rds")) %>%
select(ppid,
trial_num_in_block,
block_num, c_CueDir,
c_ResponseCorrect,
c_StimN,
c_Ecc)
library(here)
# Combine with behavioral & experimental data
data_behav <- readRDS(file.path(here(), '..', '..', 'Data', 'DataR', "fulldat_behav.rds")) %>%
select(ppid,
trial_num_in_block,
block_num, c_CueDir,
c_ResponseCorrect,
c_StimN,
c_Ecc)
rm(alldf_0)
rm(monster)
data_gaze <- bind_rows(holder, .id = 'sub_id')
data_gaze <- data_gaze %>%
filter(block_nr != "Block2", # exclude perceptual block
!reject_eeg) %>%      # exclude trials with saccades >2dva
select(time_resampled,
block_nr,
trial,
sub_id,
gaze_dev_hor,
confidence,
eye) %>%
# adapt vars
mutate(block_num = parse_number(block_nr),
trial_num_in_block = as.integer(trial),
ppid = sub_id,
.keep = "unused")
# Combine with behavioral & experimental data
data_behav <- readRDS(file.path(here(), '..', '..', 'Data', 'DataR', "fulldat_behav.rds")) %>%
select(ppid,
trial_num_in_block,
block_num, c_CueDir,
c_ResponseCorrect,
c_StimN,
c_Ecc)
data_gaze <- data_gaze %>% left_join(data_behav)
# Calculate baseline value per trial (mean value in the interval [-1.1; -0.8])
bl <- alldf_0 %>%
dplyr::filter(((time_resampled > -1.1) & (time_resampled < -0.8))) %>%
group_by(ppid, block_num, trial_num_in_block, eye) %>%
summarise(bl_val = mean(gaze_dev_hor, na.rm = FALSE)) %>%
ungroup()
data_gaze <- data_gaze %>%  left_join(bl)
# Calculate baseline value per trial (mean value in the interval [-1.1; -0.8])
bl <- data_gaze %>%
dplyr::filter(((time_resampled > -1.1) & (time_resampled < -0.8))) %>%
group_by(ppid, block_num, trial_num_in_block, eye) %>%
summarise(bl_val = mean(gaze_dev_hor, na.rm = FALSE)) %>%
ungroup()
data_gaze <- data_gaze %>%  left_join(bl)
bl <- data_gaze %>%
dplyr::filter(((time_resampled > -1.1) & (time_resampled < -0.8))) %>%
group_by(ppid, block_num, trial_num_in_block, eye) %>%
summarise(bl_val_hor = mean(gaze_dev_hor, na.rm = FALSE),
bl_val_vert = mean(gaze_dev_vert, na.rm = FALSE)) %>%
ungroup()
# Merge to one DF:
data_gaze <- bind_rows(holder, .id = 'sub_id')
data_gaze <- data_gaze %>%
filter(block_nr != "Block2", # exclude perceptual block
!reject_eeg) %>%      # exclude trials with saccades >2dva
select(time_resampled,
block_nr,
trial,
sub_id,
gaze_dev_hor,
gaze_dev_vert,
confidence,
eye) %>%
# adapt vars
mutate(block_num = parse_number(block_nr),
trial_num_in_block = as.integer(trial),
ppid = sub_id,
.keep = "unused")
data_behav <- readRDS(file.path(here(), '..', '..', 'Data', 'DataR', "fulldat_behav.rds")) %>%
select(ppid,
trial_num_in_block,
block_num, c_CueDir,
c_ResponseCorrect,
c_StimN,
c_Ecc)
data_gaze <- data_gaze %>% left_join(data_behav)
bl <- data_gaze %>%
dplyr::filter(((time_resampled > -1.1) & (time_resampled < -0.8))) %>%
group_by(ppid, block_num, trial_num_in_block, eye) %>%
summarise(bl_val_hor = mean(gaze_dev_hor, na.rm = FALSE),
bl_val_vert = mean(gaze_dev_vert, na.rm = FALSE)) %>%
ungroup()
# Merge to one DF:
data_gaze <- bind_rows(holder, .id = 'sub_id')
data_gaze <- data_gaze %>%
filter(block_nr != "Block2", # exclude perceptual block
!reject_eeg) %>%      # exclude trials with saccades >2dva
select(time_resampled,
block_nr,
trial,
sub_id,
gaze_dev_hor,
gaze_dev_vert,
confidence,
eye) %>%
# adapt vars
mutate(block_num = parse_number(block_nr),
trial_num_in_block = as.integer(trial),
ppid = sub_id,
.keep = "unused")
data_behav <- readRDS(file.path(here(), '..', '..', 'Data', 'DataR', "fulldat_behav.rds")) %>%
select(ppid,
trial_num_in_block,
block_num, c_CueDir,
c_ResponseCorrect,
c_StimN,
c_Ecc) %>%
mutate(c_Ecc = as_factor(c_Ecc),
c_StimN = as_factor(c_StimN),
c_CueDir = as_factor(c_CueDir))
data_gaze <- data_gaze %>% left_join(data_behav)
bl <- data_gaze %>%
dplyr::filter(((time_resampled > -1.1) & (time_resampled < -0.8))) %>%
group_by(ppid, block_num, trial_num_in_block, eye) %>%
summarise(bl_val_hor = mean(gaze_dev_hor, na.rm = FALSE),
bl_val_vert = mean(gaze_dev_vert, na.rm = FALSE)) %>%
ungroup()
data_gaze <- data_gaze %>%
left_join(bl) %>%
mutate(bl_val_hor = if_else(is.na(bl_val_hor), 0, bl_val_hor),
bl_val_vert = if_else(is.na(bl_val_vert), 0, bl_val_vert),
gaze_dev_hor = (gaze_dev_hor - bl_val_hor),
gaze_dev_vert = (gaze_dev_hor - bl_val_vert))
data_plot <- data_gaze %>%
mutate(gaze_dev_hor_pooled = if_else(c_CueDir == -1, gaze_dev_hor*-1, gaze_dev_hor)) %>%
# Subject means:
group_by(c_StimN, time_resampled, ppid) %>%
summarise(gaze_dev_hor = mean(gaze_dev_hor), na.rm = FALSE) %>%
# summarize across subjects:
summarise(dev_hor = mean(gaze_dev_hor, na.rm = FALSE),
sem = sd(gaze_dev_hor, na.rm = FALSE)/sqrt(21),
min = dev_hor - sem,
max = dev_hor + sem) %>%
ungroup()
tmp_plot %>%  ggplot(aes(x = time_resampled, col = c_StimN)) +
geom_line(aes(y = dev_hor)) +
geom_line(aes(y = min)) + geom_ribbon(aes(ymin=min, ymax=max, fill = c_StimN), alpha = 0.1) +
ylim(c(-0.5,0.5)) +
xlim(c(-1.1, 2.2)) +
# facet_grid(rows = 'c_CueDir') +
#  stat_summary(geom="ribbon", fun.min=min, fun.max="max", aes(fill=c_CueDir), alpha=0.3) +
theme_bw()
data_plot %>%  ggplot(aes(x = time_resampled, col = c_StimN)) +
geom_line(aes(y = dev_hor)) +
geom_line(aes(y = min)) + geom_ribbon(aes(ymin=min, ymax=max, fill = c_StimN), alpha = 0.1) +
ylim(c(-0.5,0.5)) +
xlim(c(-1.1, 2.2)) +
# facet_grid(rows = 'c_CueDir') +
#  stat_summary(geom="ribbon", fun.min=min, fun.max="max", aes(fill=c_CueDir), alpha=0.3) +
theme_bw()
data_plot <- data_gaze %>%
mutate(gaze_dev_hor_pooled = if_else(c_CueDir == -1, gaze_dev_hor*-1, gaze_dev_hor)) %>%
# Subject means:
group_by(c_StimN, time_resampled, ppid) %>%
summarise(gaze_dev_hor = mean(gaze_dev_hor), na.rm = FALSE) %>%
# summarize across subjects:
summarise(dev_hor = mean(gaze_dev_hor_pooled, na.rm = FALSE),
sem = sd(gaze_dev_hor, na.rm = FALSE)/sqrt(21),
min = dev_hor - sem,
max = dev_hor + sem) %>%
ungroup()
data_plot <- data_gaze %>%
mutate(gaze_dev_hor_pooled = if_else(c_CueDir == -1, gaze_dev_hor*-1, gaze_dev_hor)) %>%
# Subject means:
group_by(c_StimN, time_resampled, ppid) %>%
summarise(gaze_dev_hor = mean(gaze_dev_hor_pooled), na.rm = FALSE) %>%
# summarize across subjects:
summarise(dev_hor = mean(gaze_dev_hor, na.rm = FALSE),
sem = sd(gaze_dev_hor, na.rm = FALSE)/sqrt(21),
min = dev_hor - sem,
max = dev_hor + sem) %>%
ungroup()
data_plot %>%  ggplot(aes(x = time_resampled, col = c_StimN)) +
geom_line(aes(y = dev_hor)) +
geom_line(aes(y = min)) + geom_ribbon(aes(ymin=min, ymax=max, fill = c_StimN), alpha = 0.1) +
ylim(c(-0.5,0.5)) +
xlim(c(-1.1, 2.2)) +
# facet_grid(rows = 'c_CueDir') +
#  stat_summary(geom="ribbon", fun.min=min, fun.max="max", aes(fill=c_CueDir), alpha=0.3) +
theme_bw()
### STEP 6
#
data_plot <- data_gaze %>%
filter(eye == 0) %>%
mutate(gaze_dev_hor_pooled = if_else(c_CueDir == -1, gaze_dev_hor*-1, gaze_dev_hor)) %>%
# Subject means:
group_by(c_StimN, time_resampled, ppid) %>%
summarise(gaze_dev_hor = mean(gaze_dev_hor_pooled), na.rm = FALSE) %>%
# summarize across subjects:
summarise(dev_hor = mean(gaze_dev_hor, na.rm = FALSE),
sem = sd(gaze_dev_hor, na.rm = FALSE)/sqrt(21),
min = dev_hor - sem,
max = dev_hor + sem) %>%
ungroup()
data_plot %>%  ggplot(aes(x = time_resampled, col = c_StimN)) +
geom_line(aes(y = dev_hor)) +
geom_line(aes(y = min)) + geom_ribbon(aes(ymin=min, ymax=max, fill = c_StimN), alpha = 0.1) +
ylim(c(-0.5,0.5)) +
xlim(c(-1.1, 2.2)) +
# facet_grid(rows = 'c_CueDir') +
#  stat_summary(geom="ribbon", fun.min=min, fun.max="max", aes(fill=c_CueDir), alpha=0.3) +
theme_bw()
### STEP 6
#
data_plot <- data_gaze %>%
filter(eye == 1) %>%
mutate(gaze_dev_hor_pooled = if_else(c_CueDir == -1, gaze_dev_hor*-1, gaze_dev_hor)) %>%
# Subject means:
group_by(c_StimN, time_resampled, ppid) %>%
summarise(gaze_dev_hor = mean(gaze_dev_hor_pooled), na.rm = FALSE) %>%
# summarize across subjects:
summarise(dev_hor = mean(gaze_dev_hor, na.rm = FALSE),
sem = sd(gaze_dev_hor, na.rm = FALSE)/sqrt(21),
min = dev_hor - sem,
max = dev_hor + sem) %>%
ungroup()
data_plot %>%  ggplot(aes(x = time_resampled, col = c_StimN)) +
geom_line(aes(y = dev_hor)) +
geom_line(aes(y = min)) + geom_ribbon(aes(ymin=min, ymax=max, fill = c_StimN), alpha = 0.1) +
ylim(c(-0.5,0.5)) +
xlim(c(-1.1, 2.2)) +
# facet_grid(rows = 'c_CueDir') +
#  stat_summary(geom="ribbon", fun.min=min, fun.max="max", aes(fill=c_CueDir), alpha=0.3) +
theme_bw()
### STEP 6
#  Plot it
data_plot <- data_gaze %>%
#filter(eye == 1) %>%
mutate(gaze_dev_hor_pooled = if_else(c_CueDir == -1, gaze_dev_vert*-1, gaze_dev_vert)) %>%
# Subject means:
group_by(c_StimN, time_resampled, ppid) %>%
summarise(gaze_dev_hor = mean(gaze_dev_hor_pooled), na.rm = FALSE) %>%
# summarize across subjects:
summarise(dev_hor = mean(gaze_dev_hor, na.rm = FALSE),
sem = sd(gaze_dev_hor, na.rm = FALSE)/sqrt(21),
min = dev_hor - sem,
max = dev_hor + sem) %>%
ungroup()
data_plot %>%  ggplot(aes(x = time_resampled, col = c_StimN)) +
geom_line(aes(y = dev_hor)) +
geom_line(aes(y = min)) + geom_ribbon(aes(ymin=min, ymax=max, fill = c_StimN), alpha = 0.1) +
ylim(c(-0.5,0.5)) +
xlim(c(-1.1, 2.2)) +
# facet_grid(rows = 'c_CueDir') +
#  stat_summary(geom="ribbon", fun.min=min, fun.max="max", aes(fill=c_CueDir), alpha=0.3) +
theme_bw()
# Merge to one DF:
data_gaze <- bind_rows(holder, .id = 'sub_id')
data_gaze <- data_gaze %>%
filter(block_nr != "Block2", # exclude perceptual block
!reject_eeg) %>%      # exclude trials with saccades >2dva
select(time_resampled,
block_nr,
trial,
sub_id,
gaze_dev_hor,
gaze_dev_vert,
confidence,
eye) %>%
# adapt vars
mutate(block_num = parse_number(block_nr),
trial_num_in_block = as.integer(trial),
ppid = sub_id,
.keep = "unused")
head(data_gaze)
data_behav <- readRDS(file.path(here(), '..', '..', 'Data', 'DataR', "fulldat_behav.rds")) %>%
select(ppid,
trial_num_in_block,
block_num, c_CueDir,
c_ResponseCorrect,
c_StimN,
c_Ecc) %>%
mutate(c_Ecc = as_factor(c_Ecc),
c_StimN = as_factor(c_StimN),
c_CueDir = as_factor(c_CueDir))
data_gaze <- data_gaze %>% left_join(data_behav)
bl <- data_gaze %>%
dplyr::filter(((time_resampled > -1.1) & (time_resampled < -0.8))) %>%
group_by(ppid, block_num, trial_num_in_block, eye) %>%
summarise(bl_val_hor = mean(gaze_dev_hor, na.rm = FALSE),
bl_val_vert = mean(gaze_dev_vert, na.rm = FALSE)) %>%
ungroup()
data_gaze <- data_gaze %>%
left_join(bl) %>%
mutate(bl_val_hor = if_else(is.na(bl_val_hor), 0, bl_val_hor),
bl_val_vert = if_else(is.na(bl_val_vert), 0, bl_val_vert),
gaze_dev_hor = (gaze_dev_hor - bl_val_hor),
gaze_dev_vert = (gaze_dev_vert - bl_val_vert))
### STEP 6
#  Plot it
data_plot <- data_gaze %>%
#filter(eye == 1) %>%
mutate(gaze_dev_hor_pooled = if_else(c_CueDir == -1, gaze_dev_vert*-1, gaze_dev_vert)) %>%
# Subject means:
group_by(c_StimN, time_resampled, ppid) %>%
summarise(gaze_dev_hor = mean(gaze_dev_hor_pooled), na.rm = FALSE) %>%
# summarize across subjects:
summarise(dev_hor = mean(gaze_dev_hor, na.rm = FALSE),
sem = sd(gaze_dev_hor, na.rm = FALSE)/sqrt(21),
min = dev_hor - sem,
max = dev_hor + sem) %>%
ungroup()
data_plot %>%  ggplot(aes(x = time_resampled, col = c_StimN)) +
geom_line(aes(y = dev_hor)) +
geom_line(aes(y = min)) + geom_ribbon(aes(ymin=min, ymax=max, fill = c_StimN), alpha = 0.1) +
ylim(c(-0.5,0.5)) +
xlim(c(-1.1, 2.2)) +
# facet_grid(rows = 'c_CueDir') +
#  stat_summary(geom="ribbon", fun.min=min, fun.max="max", aes(fill=c_CueDir), alpha=0.3) +
theme_bw()
### STEP 6
#  Plot it
data_plot <- data_gaze %>%
#filter(eye == 1) %>%
mutate(gaze_dev_hor_pooled = if_else(c_CueDir == -1, gaze_dev_hor*-1, gaze_dev_hor)) %>%
# Subject means:
group_by(c_StimN, time_resampled, ppid) %>%
summarise(gaze_dev_hor = mean(gaze_dev_hor_pooled), na.rm = FALSE) %>%
# summarize across subjects:
summarise(dev_hor = mean(gaze_dev_hor, na.rm = FALSE),
sem = sd(gaze_dev_hor, na.rm = FALSE)/sqrt(21),
min = dev_hor - sem,
max = dev_hor + sem) %>%
ungroup()
data_plot %>%  ggplot(aes(x = time_resampled, col = c_StimN)) +
geom_line(aes(y = dev_hor)) +
geom_line(aes(y = min)) + geom_ribbon(aes(ymin=min, ymax=max, fill = c_StimN), alpha = 0.1) +
ylim(c(-0.5,0.5)) +
xlim(c(-1.1, 2.2)) +
# facet_grid(rows = 'c_CueDir') +
#  stat_summary(geom="ribbon", fun.min=min, fun.max="max", aes(fill=c_CueDir), alpha=0.3) +
theme_bw()
library(tidyverse)
path_in = file.path(path_global,
'Data',
'DataMNE',
'EEG',
'07_evokeds',
'summaries',
'CDA',
'stimon',
'global_summary')
fname_in <- file.path(path_in, 'allsubjects-mean_amp_CDA.csv')
df <- read_csv(fname_in)
here()
library(here)
here('..')
library(here)
path_global = here('../..')
path_in = file.path(path_global,
'Data',
'DataMNE',
'EEG',
'07_evokeds',
'summaries',
'CDA',
'stimon',
'global_summary')
fname_in <- file.path(path_in, 'allsubjects-mean_amp_CDA.csv')
df <- read_csv(fname_in)
View(df)
